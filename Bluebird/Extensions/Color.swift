import SwiftUI

extension Color {
    // Primary accent
    static let babyBlue = Color(red: 0.53, green: 0.81, blue: 0.92)

    // Backgrounds
    static let darkBackground = Color(red: 0.1, green: 0.1, blue: 0.15)
    static let darkElement = Color(red: 0.15, green: 0.15, blue: 0.20)

    // Text colours
    static let nearWhite = Color(red: 0.95, green: 0.95, blue: 0.96)
    static let lightGray = Color(red: 0.6, green: 0.6, blue: 0.65)

    // Self explainatory
    static let spotifyDarkGray = Color(red: 18 / 255, green: 18 / 255, blue: 18 / 255)
    static let spotifyGreen = Color(red: 30 / 255, green: 215 / 255, blue: 96 / 255)
}
